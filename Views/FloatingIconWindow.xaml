<Window x:Class="GrammrPop.Views.FloatingIconWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="GrammrPop Icon"
        Width="45"
        Height="45"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        Focusable="False"
        ShowActivated="False">

    <Grid>
        <!-- Shadow effect -->
        <Ellipse Width="32" Height="32"
                 Fill="#40000000"
                 Margin="2,3,2,1"
                 HorizontalAlignment="Left"
                 VerticalAlignment="Bottom"/>

        <!-- Main button -->
        <Button Width="32" Height="32"
                Margin="0,0,2,2"
                HorizontalAlignment="Left"
                VerticalAlignment="Bottom"
                Click="IconButton_Click"
                Cursor="Hand"
                ToolTip="Click to see corrections">
            <Button.Style>
                <Style TargetType="Button">
                    <Setter Property="Background">
                        <Setter.Value>
                            <RadialGradientBrush>
                                <GradientStop Color="#F44336" Offset="0"/>
                                <GradientStop Color="#C62828" Offset="1"/>
                            </RadialGradientBrush>
                        </Setter.Value>
                    </Setter>
                    <Setter Property="BorderBrush" Value="White"/>
                    <Setter Property="BorderThickness" Value="2"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <Grid>
                                    <Ellipse Fill="{TemplateBinding Background}"
                                            Stroke="{TemplateBinding BorderBrush}"
                                            StrokeThickness="{TemplateBinding BorderThickness}"/>
                                    <ContentPresenter HorizontalAlignment="Center"
                                                    VerticalAlignment="Center"/>
                                </Grid>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background">
                                <Setter.Value>
                                    <RadialGradientBrush>
                                        <GradientStop Color="#EF5350" Offset="0"/>
                                        <GradientStop Color="#D32F2F" Offset="1"/>
                                    </RadialGradientBrush>
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Background">
                                <Setter.Value>
                                    <RadialGradientBrush>
                                        <GradientStop Color="#C62828" Offset="0"/>
                                        <GradientStop Color="#B71C1C" Offset="1"/>
                                    </RadialGradientBrush>
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Button.Style>

            <!-- Icon content: "G" letter -->
            <TextBlock Text="G"
                      FontFamily="Segoe UI"
                      FontSize="18"
                      FontWeight="Bold"
                      Foreground="White"
                      HorizontalAlignment="Center"
                      VerticalAlignment="Center"/>
        </Button>

        <!-- Error count badge -->
        <Border x:Name="ErrorBadge"
                Width="20" Height="20"
                CornerRadius="10"
                Background="#FF9800"
                BorderBrush="White"
                BorderThickness="2"
                HorizontalAlignment="Right"
                VerticalAlignment="Top">
            <TextBlock x:Name="ErrorCountText"
                      Text="0"
                      FontSize="11"
                      FontWeight="Bold"
                      Foreground="White"
                      HorizontalAlignment="Center"
                      VerticalAlignment="Center"/>
        </Border>
    </Grid>
</Window>
